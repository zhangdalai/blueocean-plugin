pipeline {
  agent any
  stages {
    stage('Prepare') {
      agent any
      steps {
        sh 'java --version'
      }
    }
    stage('Build') {
      agent any
      steps {
        sh 'java --version'
      }
    }
    stage('Artifact') {
      agent any
      steps {
        sh 'java --version'
      }
    }
    stage('Deploy') {
      agent any
      steps {
        sh 'java --version'
      }
    }

  }
}
