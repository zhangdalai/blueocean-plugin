pipeline {
  agent any
  stages {
    stage('准备阶段') {
      agent any
      steps {
        sh 'java --version'
      }
    }
    stage('构建阶段') {
      agent any
      steps {
        sh 'java --version'
      }
    }
    stage('制品阶段') {
      agent any
      steps {
        sh 'java --version'
      }
    }
    stage('部署阶段') {
      agent any
      steps {
        sh 'java --version'
      }
    }

  }
}
